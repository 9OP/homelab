# Forward all other DNS queries to Google DNS
server=8.8.8.8
server=8.8.4.4

# Resolve all *.vestige.internal domains to Pi's Tailscale IP
# Replace this with actual Tailscale IP from: tailscale ip -4
address=/.vestige.internal/100.125.13.105

# Don't read /etc/hosts
no-hosts

# Cache DNS queries
cache-size=1000
